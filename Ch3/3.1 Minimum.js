// Write a function min that takes two arguments and returns
// their minimum.

function min(x,y)
{
    if (x < y)
    {
      // x is smaller than y
      return x;
    }
    
    else 
    {
      // y is smaller than x
      return y;
    }
}